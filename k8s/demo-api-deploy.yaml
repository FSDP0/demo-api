apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-demo-api-deploy
  namespace: api
  labels:
    app: python-demo-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: python-demo-api
  template:
    metadata:
      labels:
        app: python-demo-api
    spec:
      containers:
        - name: python-demo-api
          image: fsdp0/python-demo-api
          ports:
          - name: http
            containerPort: 3000
          resources:
            requests:
              cpu: 500m
              memory: 500M
            limits:
              cpu: 1
              memory: 1Gi

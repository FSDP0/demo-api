apiVersion: v1
kind: Service
metadata:
  name: python-demo-api-headless-svc
  namespace: api
spec:
  selector:
    app: python-demo-api
  type: ClusterIP
  clusterIP: None
  ports:
  - name: http
    port: 3000
    targetPort: http
    protocol: TCP
